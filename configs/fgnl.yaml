# Method 2-3. Reproduce with:
#   python -m src.train --model fgnl --data_index_dir data/index \
#     --out_dir results/fgnl --epochs 80 --batch_size 32 --patience 15 --num_workers 8
model: fgnl
input: mel_chunk
epochs: 80
batch_size: 32
lr: 1.0e-3
optimizer: adam
patience: 15
num_workers: 8
env: hw1_singer_env (torch 2.5.1+cu121)
